








for (var i = 0; i < bullets.length; i++) {  
  for (var j = 0; j < zombies.length; j++) {  
    if (dist(zombies[j].pos.x, zombies[j].pos.y, bullets[i].pos.x, bullets[i].pos.y) < ((13) * 3)) {
      zombies[j].splice(j, 1);
      bullets[i].splice(i, 1);
  }
}